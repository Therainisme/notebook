# GitHub

* 设置时区

`git config --global log.data local`

* push到远程仓库时记住密码

`git config --global credential.helper store`

## 协同

* 创建分支`dev`并切换至该分支

`git switch -c dev`

* 查看当前分支

`git branch`

* 切换到分支`dev`

`git swtich dev`

* 删除分支`dev`

`git branch -d dev`

* 合并分支`dev`到当前分支

`git merge dev`